# Learning
A website with a timer, stopwatch, and chat gpt for studying.  
html file in /public.  
.env and server.js in the same folder.  
api key needs to be inserted in .env.  
type npm init -y  
npm install express node-fetch dotenv  
node server.js  
into terminal  
